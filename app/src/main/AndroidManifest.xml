<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.bahasaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kursus.CourseActivity"></activity>
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".kursus.CourseLanguageListActivity" />
        <activity android:name=".dictionary.DictionaryLanguageListActivity" />
        <activity android:name=".dictionary.DictionaryActivity" />
        <activity android:name=".kuis.QuizActivity" />
        <activity android:name=".kuis.QuizListLanguageActivity" />
        <activity android:name=".kuis.StartingQuizActivity" />
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>